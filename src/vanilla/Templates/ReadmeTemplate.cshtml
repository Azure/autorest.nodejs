@using AutoRest.NodeJS.Model
@inherits AutoRest.Core.Template<CodeModelJs>
@Model.ConstructDocsHeader(EmptyLine)
@Model.GenerateTypeScriptSDKMessage()
# Microsoft Azure SDK for Node.js - @(Model.Name)
This project provides a Node.js package for accessing Azure. Right now it supports:
- **Node.js version 6.x.x or higher**
@EmptyLine
## Features
@EmptyLine
@EmptyLine
## How to Install
@EmptyLine
```bash
npm install @(Model.PackageName)
```
@EmptyLine
## How to use
@EmptyLine
### Authentication, client creation and @(Model.GetSampleMethod()?.Name) @(Model.GetSampleMethodGroupName()) as an example.
@EmptyLine
```javascript
const msRest = require("ms-rest");
@Model.GetSampleClientImport()
const token = "<access_token>";
const creds = new msRest.TokenCredentials(token);
@Model.GetSampleSubscriptionVariable()
const client = new @(Model.Name)(creds, subscriptionId);
@Model.GenerateSampleMethod(false, true)@Model.GetSampleCatchBlock()
@EmptyLine
## Related projects
@EmptyLine
- [Microsoft Azure SDK for Node.js](https://github.com/Azure/azure-sdk-for-node)